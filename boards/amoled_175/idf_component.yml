## Board-specific managed dependencies (installed only when this board is selected).
dependencies:
  waveshare/esp32_s3_touch_amoled_1_75:
    version: "3.0.1"
  # On-board QMI8658 IMU; the BSP does not expose an IMU API.
  waveshare/qmi8658:
    version: "1.0.0"
