## IDF Component Manager Manifest File
## Common dependencies only. Board-specific dependencies live in
## boards/<board>/idf_component.yml and are installed per selected board.
dependencies:
  lvgl/lvgl:
    version: "9.4.*"
    public: true
